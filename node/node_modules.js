const os = require("os");

// console.log("The amount of free memeory in the system.", os.freemem())
// console.log("The network interfaces information.", os.networkInterfaces())
// console.log("The user information for the system.", os.userInfo())

// Make a function that will retrieve all of the above pieces of information and return that information in an array.

const personalInfo = () => {
    // console.log("The amount of free memeory in the system.", os.freemem());
    // console.log("The network interfaces information.", os.networkInterfaces());
    // console.log("The user information for the system.", os.userInfo());

    let myInfo = [
        os.freemem(),
        os.networkInterfaces(),
        os.userInfo()
    ];
    return myInfo
};

console.log(personalInfo());
